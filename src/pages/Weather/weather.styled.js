import styled from 'styled-components';

export const Weather = styled.main`
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
`;

export const Container = styled.div`
  overflow-y: auto;
`;
